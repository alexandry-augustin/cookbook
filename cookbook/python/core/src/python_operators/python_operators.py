if __name__ == '__main__':

    # TODO checks that the two arguments refer to the same object in memory
    # assert( is )
    # assert( is not )
    #--------------------------------------------------
    assert(1 < 5 and 4 < 10)
    assert(2 < 5 or 9 < 4)
    assert(not(9 < 5))
    #--------------------------------------------------
    assert(7%3 == 1)
    assert(2**2==4)
    assert(1e3 == 1000)
    #--------------------------------------------------