# container of mixed types
#----------------------------------------------------------
#numeric(10)
#----------------------------------------------------------
l <- list("A", 5, TRUE, list(1,2,3))
l[[4]]
#----------------------------------------------------------
l <- as.list(NULL) # create empty list
for(i in 1:3)
	l[i] <- LETTERS[i]
